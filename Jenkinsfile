/* Requires the Docker Pipeline plugin */
pipeline {
    agent any
    stages {
        stage('Clone') {
            steps {
              git 'https://github.com/devhainguyen-94/mysite_django.git'
            }
        }
    }
}